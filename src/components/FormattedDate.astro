---
interface Props {
	date: Date;
	locale: String;
}

const { date, locale } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString(locale.toLocaleLowerCase(), {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
